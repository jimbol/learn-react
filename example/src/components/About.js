import { Container, Typography } from "@mui/material";

const About = () => {
  return (
    <Container>
      <Typography variant="h1">About</Typography>
      <Typography variant="body1">Just a simple journaling app</Typography>
    </Container>
  );
};

export default About;
